I am working on a workshop for demonstrating using AI over the API with tool calling.

I need to create 3 projects and I need you to help me with it.

1. A simple chat in the CLI enabling chat with a model using the API (in python)

2. TBCode: A simple coding agent that has three tools: list_files, read_file, and edit_file (in python based on https://ampcode.com/notes/how-to-build-an-agent)

3. TBPlanner: A simple website that has a calendar that you can add and remove events from manually bit then has a chatbot by the side that uses three tools: (list_event (given a day), add_event, remove_event) and the idea is that we can add events using the chatbot.

Project 1 and 2 should be able to use `GEMINI_API` from the env, and project 3 should be able to use `GEMINI_API` in the .env file